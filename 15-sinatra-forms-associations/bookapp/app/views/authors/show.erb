<h1> <%= @author.name %> </h1>

<p>
  <ul>
    <% @author.books.each do |book| %>
      <li>
        <a href="/books/<%= book.id %>">
          <%= book.title %>
        </a>
      <li>
    <% end%>
  </ul>
</p>


<a href='/authors'> Back to home </a>
